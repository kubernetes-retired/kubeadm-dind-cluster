#!/bin/bash
set -u -e -x
DIND_ROOT="${PWD}"
. build/funcs.sh
if [[ ${DOCKER_TAG} =~ v1.5$ ]]; then
  HYPERKUBE_URL=${HYPERKUBE_URL_1_5}
  HYPERKUBE_SHA1=${HYPERKUBE_SHA1_1_5}
  KUBEADM_URL=${KUBEADM_URL_1_5}
  KUBEADM_SHA1=${KUBEADM_SHA1_1_5}
elif [[ ${DOCKER_TAG} =~ v1.6$ ]]; then
  HYPERKUBE_URL=${HYPERKUBE_URL_1_6}
  HYPERKUBE_SHA1=${HYPERKUBE_SHA1_1_6}
  KUBEADM_URL=${KUBEADM_URL_1_6}
  KUBEADM_SHA1=${KUBEADM_SHA1_1_6}
else
  HYPERKUBE_URL=${HYPERKUBE_URL_1_7}
  HYPERKUBE_SHA1=${HYPERKUBE_SHA1_1_7}
  KUBEADM_URL=${KUBEADM_URL_1_7}
  KUBEADM_SHA1=${KUBEADM_SHA1_1_7}
fi
dind::build-image "${IMAGE_NAME}"
